    
    
    // const fetchWord = async () => {

    //   //convert to Json
    //   const words = await response.json();
    //   //Math.random() gets a number from 0-1 then multiply by
    //   //bounds then make sure it is an integer by flooring
    //   const randomWord = words[Math.floor(Math.random()*words.length)]
    //   //save in state as component must update. on initial load it won't 
    //   //be populated
    //   setSolution(randomWord);
    // };
    
          // const myArray = await words.split("\n");
      // console.log(words)
      // const words = await response.json();
      // for (const word of words) {
      //   console.log(word)
      // }
      // const randomWord = words[Math.floor(Math.random()*words.length)]